apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: velero

resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - velero-s3-credentials.yaml

#patches:
# - path: backupstoragelocation-patch.yaml
#  target:
#   kind: BackupStorageLocation
#  name: default
